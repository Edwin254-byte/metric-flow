{
  "name": "socket-client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "ts-node-dev --transpile-only --no-notify src/index.ts",
    "bld-api": "npx tsc",
    "bld-ui": "npx rimraf dist/public && cd ui && yarn install --network-timeout 100000 && yarn build && mv ./build ../dist/public && cd ../",
    "build": "yarn bld-api && yarn bld-ui"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "typescript": "^5.4.2"
  }
}
